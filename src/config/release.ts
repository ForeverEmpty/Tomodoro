export const releaseInfo = {
  "version": "0.9.0+c52f20f",
  "logs": [
    "feat(index): add tasks panel and colocate index components",
    "feat(timer): implement mode-aware countdown UI, theme toggle, and tailwind color tokens add useTimerStore with per-mode remaining time, pause/start/reset, and 4-round progress add timer components and mount timer on index page add useTheme with system/light/dark cycle and persist mode migrate colors to Tailwind theme tokens and make header fixed at top",
    "feat: scaffold tomodoro layout, routes, and tailwind theme add Tailwind Vite plugin and global theme css tokens replace starter App with RouterView and add header/layout + index/setting views wire modular routes and add storage/header config utilities remove template test/store boilerplate and allow 'index' in vue multi-word rule",
    "Initial commit"
  ]
} as const
